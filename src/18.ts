// Example of generating TypeScript code with comments and variable declarations.
let x = 5; // Declare an integer variable

function addNumbers(a: number, b: number): number {
    let result = a + b;
    console.log(`The sum of ${a} and ${b} is ${result}`);
    return result;
}

addNumbers(3, 7); // Calling the function with parameters
